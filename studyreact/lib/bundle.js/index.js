"use strict";

var _users$location;
const users = {
  nome: "Edson Gaspar"
};
let showUser = (_users$location = users.location) === null || _users$location === void 0 ? void 0 : _users$location.street;
console.log(showUser);